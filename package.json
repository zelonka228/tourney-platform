{
  "name": "tourney-platform",
  "private": true,
  "version": "1.0.0",
  "description": "Root tooling package (git hooks). Actual app code lives in frontend/ and backend/.",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
